# OneDrive / Windows
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/
*.lnk

# macOS
.DS_Store

# Editores
.vscode/
.idea/
*.swp
*~

# Node (los manuales no tienen build, pero tools/ usa node)
node_modules/
npm-debug.log*

# Temporales de autoría
*.tmp
*.bak

# Editables: no van al repositorio (decisión del dueño de marca, 17-sep-2026).
# El repo distribuye lo que se usa —SVG, PNG, HTML—, no los fuentes de diseño.
*.ai
*.psd
*.indd
*.sketch
*.fig

# Fuentes de consulta: se leen, no se distribuyen. El dato que sirve
# termina en brand-tokens.json y en el manual; el archivo se puede borrar.
Manual de marca Linex-Ultragroup OFICIAL.pdf

# PDFs sueltos de referencia/trabajo en docs/ — no van al repo.
# docs/superpowers/ (specs, planes) sigue trackeado normal.
docs/*.pdf
