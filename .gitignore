# OneDrive / Windows
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/
*.lnk

# macOS
.DS_Store

# Editores
.vscode/
.idea/
*.swp
*~

# Node (los manuales no tienen build, pero tools/ usa node)
node_modules/
npm-debug.log*

# Temporales de autoría
*.tmp
*.bak

# Editables: no van al repositorio (decisión del dueño de marca, 17-sep-2026).
# El repo distribuye lo que se usa —SVG, PNG, HTML—, no los fuentes de diseño.
*.ai
*.psd
*.indd
*.sketch
*.fig
